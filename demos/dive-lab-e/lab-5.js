const sources={article:['North Sea Cable Operations Centre','Incident bulletin · LAB FIXTURE','運用会社が障害を公表したという「発表の存在」を示す。原因の命題を自動的に確認するものではない。'],fixture:['KAWASEMI LAB fixture','Synthetic relation fixture','UI検証用の架空データ。'], 'ops-observation':['Optical monitoring system','Alarm log 02:14:22 UTC · LAB FIXTURE','光レベル低下という観測。物理損傷の原因までは確定しない。'], 'braid-cross':['Cross-domain time/place join','LAB FIXTURE','複数の時系列が同じ時間・場所に重なることを示す。近接や同時発生は因果を意味しない。'], 'origin-log':['Optical monitoring system','Primary alarm record · LAB FIXTURE','02:14:22 UTCの一次監視記録。観測の発生を確認するが、原因は示さない。'], 'operator-claim':['NorthLink Cable','Operator statement · LAB FIXTURE','「物理損傷の可能性」という主張。claims relationで、Verificationはunverified。'], 'supplier-bulletin':['Repeater supplier','Technical bulletin · LAB FIXTURE','同型機器の過去の技術注意書き。現在事件とはhistorically_similar_to/contextでありsupportではない。'], 'maintenance-plan':['Regional maintenance office','Maintenance plan MP-442 · LAB FIXTURE','同海域の計画保守記録。二つの経路の共有点だが、障害原因を確定しない。'], 'cause-log':['Operations timeline','Causal candidate notes · LAB FIXTURE','因果候補を整理するための記録。caused_byは証拠がある段階だけに限定して表示。'], 'capacity-note':['Network resilience team','Capacity note · LAB FIXTURE','障害が予備容量に与える影響を記録。affects relation。'], 'repeater-diagnostic':['Repeater diagnostics','Diagnostic excerpt · LAB FIXTURE','中継点の異常が障害区間と整合する観測。原因そのものではない。'], 'maritime-note':['Maritime authority','Initial inspection note · LAB FIXTURE','船体接触の痕跡は未確認という記録。operator claimへの限定的contradiction。'], 'ais-fixture':['AIS track fixture','Synthetic vessel track · LAB FIXTURE','位置と時刻の一致だけを示す。関連・因果・supportを意味しない。']};
function openSource(id){if(!state.sources.includes(id)){state.sources.push(id);state.persisted=true;save()}const s=sources[id]||sources.fixture;overlay(`<section class="sheet" role="dialog" aria-modal="true" aria-label="Provenance"><div class="sheet-head"><small>PROVENANCE</small><button class="icon-btn" data-close aria-label="閉じる">${icon('close')}</button></div><h2>${esc(s[0])}</h2><div class="source-card"><strong>${esc(s[1])}</strong><p>${esc(s[2])}</p><small>Static LAB fixture · no external claim</small></div></section>`)}
function openTrail(){overlay(`<section class="sheet" role="dialog" aria-modal="true" aria-label="DIVE trail"><div class="sheet-head"><small>DIVE TRAIL</small><button class="icon-btn" data-close aria-label="閉じる">${icon('close')}</button></div><h2>辿った道</h2><div class="trail-list">${state.route.length?state.route.map((r,i)=>`<button class="trail-item" data-jump="${i}"><strong>${i+1}. ${esc(r.label)}</strong><small>${esc(r.relation)}</small></button>`).join(''):'<p>まだ探索ステップはありません。</p>'}</div></section>`,true)}
function openHistory(){const unresolved=state.unresolved.length||Math.max(0,3-state.discoveries.length);const branches=state.journey.filter(x=>x.parentStepId&&state.journey.filter(y=>y.parentStepId===x.parentStepId).length>1).length;overlay(`<section class="sheet" role="dialog" aria-modal="true" aria-label="Session history"><div class="sheet-head"><small>SESSION HISTORY</small><button class="icon-btn" data-close aria-label="閉じる">${icon('close')}</button></div><h2>${demos[current-1].name}</h2><div class="history-meta"><div class="metric"><strong>${state.journey.length}</strong>visited steps</div><div class="metric"><strong>${state.sources.length}</strong>sources inspected</div><div class="metric"><strong>${state.discoveries.length}</strong>discoveries</div></div><button class="history-item" data-resume><strong>RESUME · ${esc(state.resume||'anchor')}</strong><small>最後に見ていた地点へ戻る</small></button><div class="history-item"><strong>BRANCH CHOICES · ${branches}</strong><small>BACK後に別ルートへ進んでも、以前の訪問ステップは消さない。</small></div><div class="history-item"><strong>未解決の道 ${unresolved}</strong><small>AIが「理解した」とは判定しない。未探索・保留のルートだけを記録。</small></div>${state.discoveries.map(x=>`<div class="history-item"><strong>DISCOVERY · ${esc(x)}</strong><small>ユーザーが辿った結果として記録</small></div>`).join('')}</section>`,true)}
function overlay(html,jump=false){document.getElementById('overlay').innerHTML=`<div class="sheet-backdrop" data-close></div>${html}`;document.querySelectorAll('[data-close]').forEach(x=>x.onclick=closeOverlay);document.querySelector('[data-resume]')?.addEventListener('click',()=>{state.stage='explore';closeOverlay();render()});if(jump)document.querySelectorAll('[data-jump]').forEach(x=>x.onclick=()=>{const idx=Number(x.dataset.jump);state.route=state.route.slice(0,idx+1);state.depth=state.route[idx]?.depth||idx+1;closeOverlay();save();render()})}
function closeOverlay(){document.getElementById('overlay').innerHTML=''}
render();